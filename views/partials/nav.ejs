<nav id="navigation" class="navbar rounded">

  <div id="containerNav">
    <a id="brand" class="navbar-brand" href="/"><i class="fas fa-paw"> pawsЯus</i></a>
  </div>

  <div id="containerNav">
    <!-- LOGGED IN -->
    <% if (currentUser) { %>
    <li class="nav-item dropdown" style="list-style-type: none;">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown"
        aria-expanded="false">
        <%=currentUser.username%>
      </a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <form action="/sessions?_method=DELETE" method="POST">
          <!-- <input type="submit" value="Log out"> -->
          <button class="dropdown-item">Log out</button>
        </form>
      </ul>
    </li>


    <!-- LOGGED OUT -->
    <% } else { %>

    <a class="btn btn-outline-success" href="/users/new">Sign Up <i class="fas fa-user-plus"></i></a>
    <a class="btn btn-outline-primary" href="/sessions/new">Log In <i class="fas fa-sign-in-alt"></i></a>
    <% } %>
  </div>
</nav>